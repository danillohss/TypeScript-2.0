const coisas = ["CORDA", "PILHA", "LAMPADA", "CHAVE"]

export class pessoa {
    nome: string;
    altura: number;
    constructor(nome: string, altura: number) {
        this.nome = nome;
        this.altura = altura;
    }
}